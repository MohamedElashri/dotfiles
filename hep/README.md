# HEP clusters

Run `../install.sh --profile hep` from this directory, or `./install.sh --profile hep`
from the repository root. Bash loads `common.bash`, then selects a cluster file
at startup through `select.bash`. No packages, root privileges or Zsh are needed.

See the root README for shared-home selection, overrides and migration.
