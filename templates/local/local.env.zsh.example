# Copy to ~/.config/dotfiles/local.env.zsh. Loaded in login and interactive Zsh
# before tool initialization. Keep this free of output and interactive hooks.
# export EDITOR=nvim
# export ZSH_THEME=robbyrussell
# [[ -d "$HOME/.local/host-bin" ]] && path=("$HOME/.local/host-bin" $path)
